# =============================================================================
# Donna Life OS - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values

# =============================================================================
# Authentication Configuration
# =============================================================================

# Enable authentication (set to "true" for production deployments)
AUTH_ENABLED=false

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Add authorized redirect URI: https://your-domain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Session secret key (generate a random string, e.g., using: openssl rand -hex 32)
# IMPORTANT: Use a strong, unique value in production!
SESSION_SECRET_KEY=change-me-to-a-random-string-in-production

# Base URL for OAuth callbacks (required for production)
# Example: https://donna.yourdomain.com
# AUTH_BASE_URL=https://your-domain.com

# Set to "true" if your site uses HTTPS (recommended for production)
AUTH_HTTPS_ONLY=false

# Restrict access to a specific email address (recommended for personal use)
# Takes priority over ALLOWED_EMAIL_DOMAINS if set
# Example: your-email@gmail.com
# ALLOWED_EMAIL=

# Restrict access to specific email domains (comma-separated, optional)
# Leave empty to allow all authenticated Google users
# Example: yourdomain.com,company.org
# ALLOWED_EMAIL_DOMAINS=

# Session duration in seconds (default: 604800 = 7 days)
# SESSION_MAX_AGE=604800

# =============================================================================
# Claude API (required for Donna agent)
# =============================================================================

# Your Anthropic API key
ANTHROPIC_API_KEY=your-anthropic-api-key
